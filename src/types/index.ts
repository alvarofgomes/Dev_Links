export interface Perfil {
  nome: string;
  usuario: string;
  status: string;
  avatar: string;
}

export interface LinkDestaque {
  url: string;
  icone: string;
  titulo: string;
  descricao: string;
}

export interface RedeSocial {
  nome: string;
  url: string;
  icone: string;
}
